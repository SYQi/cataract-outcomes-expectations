# Staff password for /admin and CSV export (defaults to 123 if unset)
ANALYTICS_ADMIN_PASSWORD=123

# Required on Vercel for durable session storage (create a Blob store in the Vercel dashboard)
# Locally, sessions are saved under /tmp/patient-conversion-sessions instead.
BLOB_READ_WRITE_TOKEN=
